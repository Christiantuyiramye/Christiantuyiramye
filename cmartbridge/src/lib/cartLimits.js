// Shared cart caps — imported by both the frontend (input maxes) and the backend
// (authoritative validation), so the two can never silently drift apart.
export const MAX_QTY_PER_LINE = 20
export const MAX_CART_LINES = 30
