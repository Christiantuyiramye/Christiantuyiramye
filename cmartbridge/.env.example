MOMO_BASE_URL=https://sandbox.momodeveloper.mtn.com
MOMO_SUBSCRIPTION_KEY=replace_me
MOMO_API_USER=replace_me_uuid
MOMO_API_KEY=replace_me
MOMO_TARGET_ENVIRONMENT=sandbox

# MTN's sandbox only accepts EUR for test requesttopay calls — not a bug.
# Switch to RWF once MOMO_TARGET_ENVIRONMENT=mtnrwanda (production).
MOMO_CURRENCY=EUR

PORT=4000
