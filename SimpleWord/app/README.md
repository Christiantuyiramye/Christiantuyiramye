# Simple Word — Flutter app

The Android + Windows app. See ../README.md for the full project and
../content/ for the database it reads.

Before first build: copy `../content/output/bible.sqlite` into `assets/`
(see assets/README.md). Then `flutter analyze && flutter test && flutter run`.
